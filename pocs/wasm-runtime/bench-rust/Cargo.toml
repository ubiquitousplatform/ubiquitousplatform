[package]
name = "bench-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
extism = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "wasm_bench"
harness = false
